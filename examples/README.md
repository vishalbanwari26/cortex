# Examples

To run the live VLM path, drop a scene photo here as `scene.jpg` and run:

```bash
python -m cortex.cli "Tidy the table." --live --image examples/scene.jpg
```

The offline demo needs no image:

```bash
python -m cortex.cli "Put the red mug in the cupboard."
```
